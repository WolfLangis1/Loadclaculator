# Load Calculator Environment Configuration
# Copy this file to .env and add your actual API keys

# API Configuration
# Enable real satellite data (set to 'true' to use real APIs)
USE_REAL_AERIAL_DATA=false

# Supabase Configuration (for authentication)
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Server-side Google Maps API Key (for Vercel serverless functions)
# Same key as above but used in backend API endpoints
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Mapbox API Key (alternative to Google Maps)
# Get from: https://account.mapbox.com/access-tokens/
MAPBOX_API_KEY=your_mapbox_api_key_here

# Preferred aerial imagery provider ('google' or 'mapbox')
AERIAL_PROVIDER=google

# Development Notes:
# - Never commit your actual API keys to version control
# - Add .env to .gitignore (already included)
# - For production, set these as environment variables in your hosting service
# - Google Maps: ~$2 per 1,000 satellite images + $5 per 1,000 geocoding requests
# - Mapbox: ~$0.50 per 1,000 requests for both imagery and geocoding